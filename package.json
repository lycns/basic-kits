{
  "name": "basic-kits",
  "private": true,
  "workspaces": [ "packages/*" ],
  "scripts": {
    "init": "yarn install && lerna bootstrap && lerna run build",
    "build:prod": "lerna run build && lerna publish",
    "clean": "lerna clean -y && rm -rf node_modules",
    "clean:init": "npm run clean && npm run init"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
